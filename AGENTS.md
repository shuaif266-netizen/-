# AGENTS

- Use `npm`, not other package managers.
- Read `README.md` before making changes.
- Every API response must pass Zod validation before returning.
- Never write API keys or secrets into source code.
- Keep each task scoped to the module you own.
- Before finishing, run `npm run lint` and `npm run typecheck`.

